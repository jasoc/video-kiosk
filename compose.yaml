services:
  video-kiosk:
    build: .
    image: video-kiosk:latest
    container_name: video-kiosk
    restart: unless-stopped
    ports:
      - "${PORT}:8080"
    volumes:
      - ${VIDEO_DIR}:/app/videos
